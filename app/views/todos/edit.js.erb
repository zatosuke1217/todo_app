$("#modalArea").html(
  "<%= j(render 'modal_form', todo: @todo, goal: @goal) %>"
);

$("#modalArea").find(".modal").on('shown.bs.modal', () => {
  $("#modalArea").find('form [name="todo[content]"]').eq(0).focus();
})

$("#modalArea").find(".modal").modal("show");